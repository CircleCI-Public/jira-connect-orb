description: Notify Jira on single job
usage:
  version: 2.1
  orbs:
    jira: circleci/jira@x.y.z

  workflows:
    build:
      jobs:
        - build:
            post-steps:
              - jira/notify

  jobs:
    build:
      docker:
      - image: circleci/node:16
      steps:
        - run: echo "hello"
